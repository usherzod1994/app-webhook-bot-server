package com.example.appwebhookbotserver.repository;

import com.example.appwebhookbotserver.entity.ChannelAndGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChannelAndGroupRepository extends JpaRepository<ChannelAndGroup, Integer> {
}
